const menu=document.querySelector('.menu');const nav=document.querySelector('nav');const setMenu=open=>{nav?.classList.toggle('open',open);menu?.setAttribute('aria-expanded',String(open));if(menu){menu.textContent=open?'×':'☰';menu.setAttribute('aria-label',open?'Fechar menu':'Abrir menu')}};menu?.addEventListener('click',()=>setMenu(!nav?.classList.contains('open')));nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',event=>{if(event.key==='Escape')setMenu(false)});document.addEventListener('click',event=>{if(nav?.classList.contains('open')&&!nav.contains(event.target)&&!menu?.contains(event.target))setMenu(false)});
document.querySelector('#quote-form')?.addEventListener('submit',event=>{event.preventDefault();const form=new FormData(event.currentTarget);const text=`Olá! Gostaria de solicitar um orçamento com a Pratika Sport.\n\nNome: ${form.get('nome')}\nTelefone: ${form.get('telefone')}\nCidade/UF: ${form.get('cidade')}\nProjeto: ${form.get('projeto')}\nDetalhes: ${form.get('mensagem')||'Não informado'}`;document.querySelector('#form-status').textContent='Abrindo o WhatsApp para você revisar e enviar a mensagem…';window.open(`https://wa.me/5515997157642?text=${encodeURIComponent(text)}`,'_blank','noopener,noreferrer')});
